# 增值税及附加税费申报表

这是从 2025 年 12 月 PDF 第一页网页化的静态表单，入口文件为 `index.html`。

## GitHub Pages

将本目录推送到 GitHub 仓库后，在仓库设置中选择 **Settings → Pages → Deploy from a branch → `main` / `/ (root)`**，等待构建完成即可访问。

页面不依赖构建工具或第三方运行时，支持直接打开 `index.html`、编辑金额、恢复 PDF 原值，以及通过浏览器打印为 PDF。
